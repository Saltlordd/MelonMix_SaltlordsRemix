name: Cooking Mama v3 - Build Lock On APK

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/cooking-mama-v3.yml
      - MelonMix-KH-Lock-On-touch-v1.patch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Collect the full source
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Android tools
        uses: android-actions/setup-android@v3

      - name: Install stable Android build tools
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager \
            "platforms;android-36" \
            "build-tools;36.0.0" \
            "ndk;28.0.13004108" \
            "cmake;3.22.1"

      - name: Configure the stable Android SDK
        run: sed -i 's/compileSdkVersion = 37/compileSdkVersion = 36/' buildSrc/src/main/kotlin/AppConfig.kt

      - name: Add the KH Lock On touchscreen button
        run: git apply MelonMix-KH-Lock-On-touch-v1.patch

      - name: Bake the debug APK
        run: |
          chmod +x gradlew
          ./gradlew :app:assembleGitHubProdDebug --stacktrace

      - name: Package the APK for download
        uses: actions/upload-artifact@v4
        with:
          name: MelonMix-KH-Lock-On-v1
          path: app/build/outputs/apk/gitHubProd/debug/*.apk
          if-no-files-found: error
          retention-days: 7
